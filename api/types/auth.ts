export type UserData = {
    id: number;
    name: string;
    email: string;
}

export type SignupCredentials = {
    name: string;
    email: string;
    password: string;
    confirmPassword: string;
}